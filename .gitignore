__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
build/
dist/
artifacts/runs/
# Job-level checkpoints are a derived cache, keyed by a configuration fingerprint and
# regenerable by re-running. They also include pickles, which execute code on load — not
# something a repository should hand a reader.
artifacts/checkpoints/

# Scratch worktrees used for A/B benchmarking against another branch.
base-wt/
